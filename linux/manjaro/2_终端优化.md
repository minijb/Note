# 终端优化

### 1. 开启ssh服务

```shell
pacman -Sy openssh 

#自己选择
systemctl enable sshd.service #开机启动
systemctl start sshd.service #立即启动
systemctl restart sshd.service# 立即重启
```

### 2. 安装ohmyzsh

官网https://ohmyz.sh/#install

```shell
#安装zsh
sudo pacman -S zsh 
#查看ip
ip addr
#开启git的代理
git config --global http.proxy 'socks5://127.0.0.1:1086'
git config --global https.proxy 'socks5://127.0.0.1:1086'
#共享剪切板
sudo pacman -S open-vm-tools
sudo pacman -S xclip

#安装
sh -c "$(curl -fsSL https://raw.github.com/ohmyzsh/ohmyzsh/master/tools/install.sh)"
```

选择theme

**Powerlevel10k**

https://github.com/romkatv/powerlevel10k#oh-my-zsh

```shell
git clone --depth=1 https://gitee.com/romkatv/powerlevel10k.git ${ZSH_CUSTOM:-$HOME/.oh-my-zsh/custom}/themes/powerlevel10k

#Set ZSH_THEME="powerlevel10k/powerlevel10k" in ~/.zshrc
```

10k在终端第一次启动的时候会自动开启配置

如果部门可以使用一下命令进行重新配置

```shell
p10k configure
```

### 3. 字体安装

使用nerdfonts安装字体

https://github.com/ryanoasis/nerd-fonts

我使用的字体https://www.programmingfonts.org/#roboto

`Option 2: Release Archive Download`

下载自己喜欢的字体，在文件管理系统中双击安装，随后重启系统

### 4. zsh插件安装

#### colored-man-pages

```shell
plugins=(... colored-man-pages)
```

#### z

快速跳转，会根据我们到过的目录，使用`z {file}`来快速跳转

```shell
plugins=(... z)
```

#### zsh-autosuggestions

https://github.com/zsh-users/zsh-autosuggestions

**安装**

```shell
git clone https://github.com/zsh-users/zsh-autosuggestions ${ZSH_CUSTOM:-~/.oh-my-zsh/custom}/plugins/zsh-autosuggestions

plugins=( 
    # other plugins...
    zsh-autosuggestions
)
```

#### wd 

书签，可以把当前文件夹设置为书签，以此来快速跳转

https://github.com/ohmyzsh/ohmyzsh/tree/master/plugins/wd

```shell
plugins=(... wd)
```

#### sudo

双击esc添加sudo

```shell
plugins=(... sudo)
```

#### zsh-syntax-highlighting

https://github.com/zsh-users/zsh-syntax-highlighting

1. Clone this repository in oh-my-zsh's plugins directory:

   ```
   git clone https://github.com/zsh-users/zsh-syntax-highlighting.git ${ZSH_CUSTOM:-~/.oh-my-zsh/custom}/plugins/zsh-syntax-highlighting
   ```

2. Activate the plugin in `~/.zshrc`:

   ```
   plugins=( [plugins...] zsh-syntax-highlighting)
   ```

> source .zshrc 直接重新配置zsh

### 5. 包管理工具

homebrew！！

https://brew.sh/index_zh-cn

**安装**

```shell
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

#运行命令
test -d ~/.linuxbrew && eval "$(~/.linuxbrew/bin/brew shellenv)"
test -d /home/linuxbrew/.linuxbrew && eval "$(/home/linuxbrew/.linuxbrew/bin/brew shellenv)"
test -r ~/.bash_profile && echo "eval \"\$($(brew --prefix)/bin/brew shellenv)\"" >>~/.bash_profile
echo "eval \"\$($(brew --prefix)/bin/brew shellenv)\"" >>~/.profile
```

### 6. ranger安装！！

