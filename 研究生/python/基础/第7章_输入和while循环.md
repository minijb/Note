## 输入

`message = input("xxxxx")`实参为想要输出的东西

返回一个输入的东西

input的输入的内容是字符串，因此如果输入的是数字的话

- 使用`int([string])`来将字符串转化为数字

## while循环

循环删除同一元素

```python
while 'cat' in list:
    list.remove('cat')
```

> 0   0.0  '' 会被认为是false  其余为true
