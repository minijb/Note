---
title: 常用软件安装
date: 2026-03-21
tags:
  - knowledge
type: knowledge
aliases:
  -
description: sudo apt install curl
draft: false
---

# 常用软件安装

- [[StartUp_常用工具_NVM]]

```sh
export WIN_IP=$(ip route show default | awk '{print $3}')
export http_proxy=http://$WIN_IP:7897
export https_proxy=http://$WIN_IP:7897

sudo apt update
sudo apt upgrade

# curl
sudo apt install curl

# git
sudo apt install git
git config --global user.name minijb
git config --global user.email 1414309848@qq.com

# 安装 NVM
sudo apt install libatomic1
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.3/install.sh | bash

# 加载环境变量
source ~/.bashrc

# 安装最新版的node
nvm install nodejs 
```



## AI 部分

```sh
npm install -g @anthropic-ai/claude-code

# 模型管理工具
curl -fsSL https://github.com/SaladDay/cc-switch-cli/releases/latest/download/install.sh | bash
```

见 https://github.com/SaladDay/cc-switch-cli/blob/main/README_ZH.md
